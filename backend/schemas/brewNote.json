{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "http://example.com/example.json",
    "type": "object",
    "title": "The brew note schema",
    "description": "The brew note schema.",
    "default": {},
    "examples": [
        {
            "brewid": 1,
            "brewnotebody": "Took a reading, no change. Will wait one week and check again."
        }
    ],
    "required": [
        "brewid",
        "brewnotebody"
    ],
    "properties": {
        "brewid": {
            "$id": "#/properties/brewid",
            "type": "integer",
            "title": "The brewid schema",
            "description": "An explanation about the purpose of this instance.",
            "default": 0,
            "examples": [
                1
            ]
        },
        "brewnotebody": {
            "$id": "#/properties/brewnotebody",
            "type": "string",
            "title": "The brewnotebody schema",
            "description": "An explanation about the purpose of this instance.",
            "default": "",
            "examples": [
                "Took a reading, no change. Will wait one week and check again."
            ]
        }
    },
    "additionalProperties": false
}